# DSH Bundle patch: install the provider-usage runtime row and its browser half.
# Users can override this row later from their profile cordis.patch.yml.
- insert:
    - id: ui-provider-usage
      name: '@local/dsh-provider-usage'
